import 'package:flutter/material.dart';

class ProjectDescScreen extends StatelessWidget {
  const ProjectDescScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
